# Same Supabase project as the mobile app; both values are safe to expose to
# the browser (RLS is the security boundary). Copy to .env.local and fill in.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
